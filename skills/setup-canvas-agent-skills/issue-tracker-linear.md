# Issue Tracker: Linear

Use Linear for PRDs, issue slices, dependency links, and status updates.

## Mechanics

Record the exact Linear workflow for this repo:

- Team key/name:
- Project/initiative:
- Statuses:
- Labels:
- How to create issues:
- How to link blockers:
- How to attach PRD/DAG/evidence links:

When using the Linear app/tools, create blockers before blocked work so real issue identifiers can be referenced.

## Dependencies

- Use Linear blocking relations when available.
- Also write dependency information in the issue body so workers can read it without relying on UI state.

## Status

Map workflow states to:

- `ready-for-agent`
- `in-progress`
- `review-requested`
- `changes-requested`
- `done`
