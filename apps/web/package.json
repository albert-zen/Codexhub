{
  "name": "@codexhub/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @codexhub/core build && tsc -p tsconfig.json && node scripts/require-child-process-spawn.mjs build && vite build",
    "check": "pnpm --filter @codexhub/core build && tsc -p tsconfig.json --noEmit",
    "dev": "vite --host 127.0.0.1 --port 4318",
    "test": "pnpm --filter @codexhub/core build && node scripts/require-child-process-spawn.mjs test && vitest run"
  },
  "dependencies": {
    "@codexhub/core": "workspace:*",
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.2.7",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
